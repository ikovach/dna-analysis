# ClinVar database (downloaded automatically, ~180 MB)
clinvar/

# User data — raw VCF, extracted variants, results
data/

# Raw sequencing files that might be in project root
*.vcf
*.vcf.gz
*.bam
*.fastq
*.fastq.gz
*.fq.gz

# Lab reports
*.pdf

# macOS
.DS_Store
